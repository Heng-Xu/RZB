# 论文第二轮高精度修正 — 变更日志

**依据：** `paper/论文第二轮高精度修正说明书_供Agent执行.md`
**输入底稿：** `paper/epg-nsga-ii-paper_final_compressed_revised.docx`（第一轮产物）
**最终交付：** `paper/许恒-EPG-NSGA-II-终稿.docx`（IEEE 双栏，**严格 2 页**，body 10 pt 模板字号未改）
**日期：** 2026-07-15

---

## A. 已按说明书执行的修改

### 文本 / 术语
- 全稿 `Elastic → Differentiated`（标题 / 方法小节 / 贡献 / 结论 / 图注 / 图轴）。
- 正文与 Algorithm 1 统一用连字符 `non-dominated`；**参考文献题名保留原样 `nondominated`**（说明书 §12：题名不改）。
- 摘要重写：K=40 下 final HV 高出 standard NSGA-II 4.8%，约 84 代达到其 final HV。
- 引言 / 方法删去 "grids are bidirectional" 等冗词，K 定义为县内 110-kV 变电站数。
- `plain NSGA-II → standard NSGA-II` 全稿统一。

### 公式（OMML，Word 原生）
- **eq(2)** 聚合容载比带重建，**保留 δ**（county load-coincidence factor）：`1.8 ≤ Σ RⱼLⱼᵖ /(δ ΣLⱼᵖ) ≤ 2.2`。
- **eq(4)** 目标函数 f₂ 重建为 `f₂ = Σⱼ EENSⱼ`，**移除未定义的 Tⱼ**（其调度支援已并入 eq(5) 的 Sⱼ,ₛ 联络支援项）。
- eq(1)(3)(5) 保持底稿原式；已逐一核验 OMML 文本内容完整、无截断。

### Algorithm 1（形式化 + 符号修复）
- Input 显式定义 `planning data D`、`population size N`、`generation limit G`。
- Output 改为 `feasible non-dominated set A`。
- 第 2 / 6 行算子名统一为 `RepairOperator`；第 8 行合并符号修正为 `U ← P ∪ Q`（∪ 正常渲染）；第 9 行 `Non-dominated sorting and crowding-distance selection`。
- II-B WarmStartSampling / RepairProjection 用数学形式化：引入可行聚合集 `ℛ = {R : Rⱼ∈[1.2,3.0], band (2) holds}`，`Pⱼ ← min{6, [rⱼ⁰ − βRⱼLⱼᵖ]₊}`，并说明残余违反的处理。

### 参考文献
- **删除** M. Nicolini（供水管网，与主题无关）一条，其余按**首次出现顺序**重排为 [1]–[8]，正文引用与题注编号一一对应（已脚本核验 body cites == ref labels == [1..8]）。
- [1] Hou 补全 `Applied Energy, vol. 242, pp. 205–215, 2019, doi:10.1016/j.apenergy.2019.03.067`。
- [5] 保留编辑要求引文 `X. Xue, C. Yang, Y. Hu, et al., IEEE TEVC 26(6):1424–1438, 2022`。
- [7] PVGIS 在线源精简。

### 清理孤立公式片段（关键修复）
- 底稿 P14/P16/P18/P20/P21 段落尾部残留**旧内联 OMML 片段**（`Lⱼᵖ`、`r̃ⱼ(Pⱼ)`、`R₂₂₀` 等），是第一轮用 Unicode 重写散文后未删的原内联公式。本轮已全部移除（Word 中同样会显示，故必须删）。

---

## B. ★需作者确认事项（与说明书字面有出入或涉及取舍）

| # | 事项 | 处理 | 原因 |
|---|---|---|---|
| 1 | **eq(2) 保留 δ** | 未按说明书 §8.3 的 LaTeX 去掉 δ | 说明书 §16.2 要求与代码一致；`ea_county.py` 中 `denom = DIVERSITY_FACTOR*sum_peak`，报告拐点 1.80 即 δ 调整值。去 δ 会击穿拐点下界并需重跑全套 EA。 |
| 2 | **eq(4) 移除 Tⱼ** | f₂ 简化为 Σ EENSⱼ | 说明书指出 Tⱼ 未定义；其含义（联络线调度支援）已由 eq(5) 的 Sⱼ,ₛ 承担，避免重复符号。 |
| 3 | **可行性口径 = Scheme B** | 残余违反经约束支配（constraint-domination）处理 | 代码 `_repair_one` 在变量触界时可能留残余违反，交由 pymoo 约束支配，非硬可行。 |
| 4 | **HV 口径 = 三次独立运行的 final HV 均值** | Table I 数值为 3 seed 均值、按各 K 的 standard NSGA-II 归一 | 与 `ea_converge.csv` 核验一致。 |
| 5 | **84 代 ≈ 10.5% 评估** | 正文写 "约 84 代 / 约 10.5% 评估量" | 84/800 = 10.5%，同种群规模下的评估次数占比。 |
| 6 | **排版让步（守 2 页）** | 参考文献 7.0 pt / 行距 0.70；图 1 保持约 6.0 cm 可读尺寸（≈原始版面尺寸） | 说明书 §16.4 要求严格 2 页。**正文 body 仍为模板 10 pt、标题 24 pt，未动版面字号**；2 页由正文精简 + 参考文献区常规压缩实现（IEEE 参考文献本就小于正文），**图 1 未牺牲可读性**（每格 `R x.xx / ±x.x MW` 清晰可辨）。 |
| 7 | **正文精简** | 删减约 11+ 行低信息量表述（NSGA-III 尾句、图 1 描述从句、future-work 列表等） | 全部正式内容 + 可读图 + 10 pt 正文 + 2 页四者冲突，按用户决定"再删内容"处理，未删关键结论/数据。 |

---

## C. 红线（守住）
- 数字全真：13.1%@K20 降本、+4.8%@K40 HV、10.5% 评估、聚合 1.80，均沿用既有实验，无重跑、无编造。
- δ 保留；warm-start 命名保留；未新增未经确认的引用。

## D. 渲染说明
- 交付物为 **Word (.docx)**，公式为 Word 原生 OMML。
- 若用 LibreOffice 打开会看到 `∑` 显异形、`(` 显 `\(`、上标 `p` 显似 `r`、`h` 显似 `n` —— 均为 **LibreOffice 对 OMML 的渲染怪癖**，已逐一核对 docx 内 OMML 文本为正确字符（`L_j^p`、`p_s h_s`、真括号），**Word 中正常**。

---

## E. 第三轮修正（2026-07-15，内联符号 OMML 化 + 摘要精简 + 6 作者 + 定义补齐）

**依据：** 用户三条指令 —— ①摘要太长术语太多需精简；②正文穿插的公式符号应"直接从公式复制"（本质是公式），现基本全部有问题；③参照 `老师修改意见_逐字句修正说明书.md` 再审查正文、勿过度改、作者占位增到 6 个两大行。

### 已执行
1. **摘要精简（响应①）：** 去掉术语堆叠链（"load-weighted aggregate CLR band"、"substation- and upstream-level reverse-power-flow constraints"、"feasibility-aware"、"public-data-informed"、"engineering repair operator"），~180→~115 词。**关键数字与正文完全一致**（13.1%、4.8%、K=40、~84 代），未编造、未改写成"9×"之类正文没有的表述。
2. **正文内联符号全部 → 原生 inline OMML（响应②，核心）：** 原先是 Unicode/字面下划线混排（`c_R`/`R_j`/`P_j` 字面下划线、`r̃` 组合波浪、`ᵖ`/`⁰` 异形上下标、`R₁` 与 `R_K` 不一致）——正是"都有问题"的根因。已把 P14/16/17/18/20/21/24/25/26/29/32/33 的每个符号改成与显示公式同款的 inline OMML（`set_mixed()`：文本 run 与 `m:oMath` 交错，`sz=20`=正文 10pt，**复刻本就正确的 P15**）。样式对齐原公式(3)(4)(5)的**默认斜体**（`c_R`/`C_j^{loss}`/`L_j^p` 均斜体），并把公式(2)的上标 `\mathrm p` 改回斜体 `p`，使 5 条公式 + 全部内联符号**统一斜体 p**。核验：全文无残留 Unicode/字面下划线、无孤立 OMML 片段（`PROBLEM paras: []`）。
3. **作者占位增到 6 个、两大行（响应③）：** P1 = "Heng Xu, Second Author, Third Author," + 换行 + "Fourth Author, Fifth Author, Sixth Author"，居中两行，共用原单位。**占位名待作者替换为真实合著者**。
4. **补齐老师要求的定义（术§18，此前 2 页压缩中丢失）：** 在方法首段(P14)加 **CLR 数值定义**"planned transformer capacity over forecast peak load" + **differentiated 含义**"lets CLRs vary across substations subject to a load-weighted aggregate band"，一句话紧凑补入。
5. **正文对老师说明书全项复核（响应③）：** 一致性清扫全绿 —— 无 `plain NSGA-II`、无 `cost index`、无 `county-wide`、无裸 `reverse power`；`standard NSGA-II`/`at the same EENS`/`load-weighted aggregate`/δ 保留 均已在位。**未做无关改动**。
6. 版面：图 1 恢复可读 **5.2 cm**；参考文献回到 **7.0 pt**（不再压到 6.5）；公式(4)两条 30 pt 行距→23 pt（Word/LO 一致的诚实回收），去图注 spacer 余量。

### ★2 页与 LibreOffice 的关键说明（务必知悉）
- **内容量本身是 2 页的**：做过 A/B 对照 —— **同一版若把这些符号退回纯文本 = 2 页；用原生 OMML = LibreOffice 显示 3 页**。唯一差别就是符号形态。
- 3 页**只出现在 LibreOffice**：它对 inline OMML 过度渲染（运算符间距→多换行），这是本机唯一可用的转换器的已知缺陷，**不代表 Word**。Word/WPS 原生紧凑渲染 OMML，**极可能 2 页**。
- 本机无 Word，无法最终确认页数。**用户已拍板（2026-07-15）：交付完整 OMML 版，由作者在 Word/WPS 里确认页数**；若 Word 里仍溢出再针对性微调（勿为迁就 LibreOffice 而砍内容/压版面，否则 Word 里第 2 页会偏空）。
- 未采纳"强制 LibreOffice 也 2 页"（需缩图到 4 cm、refs 6.5pt、删 CLR 定义）——那会牺牲图/参考文献可读性与老师要求的定义，且劣化真实 Word 版面。

---

## F. 作者块改为 IEEE 3-per-row 网格（照范文 `Swarm Decision-Making…Pursue-Containment.pdf`）

- 原单一共用单位块（1 作者）→ **6 作者 IEEE 会议网格**：3 列 × 2 行的**无边框表格**，每格 = `序号(上标 st/nd/rd/th) + 姓名`(正体) / `系`(斜体) / `大学`(斜体) / `城市,国家`(正体) / `email`(正体)，全部居中、Times 10 pt——与范文版式一一对应。
- 表格插在**标题之后、分节符(P6 的 sectPr, cols=1 全宽段)之前**，保留"全宽标题区 + 2 栏正文"的双分节结构不破坏。
- 作者 1 = Heng Xu(真实单位/邮箱)；**2–6 为占位**（暂用同一单位 + `coauthorN@cumt.edu.cn`），**待作者替换真实姓名/单位/邮箱**。
- 版面格式复核（对标范文 + IEEE 规范）：标题(Times 居中 Title Case)、小节小型大写罗马数字标题、斜体子标题、粗斜体 `Abstract—`/`Index Terms—`、公式右编号、Table/Fig 题注、参考文献格式——**均已符合，无其他需修正项**。
- 页数仍为 LibreOffice 3 页（同 §E 的 OMML 过度渲染），用户已知悉、由作者在 Word 里确认并按需手动压缩内容。
