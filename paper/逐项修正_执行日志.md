# 《许恒-EPG-NSGA-II-终稿》逐项修正说明书 — 执行日志

**依据：** 用户《逐项修正说明书》（Step 1–12 + 公式硬约束 §1–§10）
**交付物：** `paper/许恒-EPG-NSGA-II-终稿.docx`（Word，公式全部原生 OMML）
**构建脚本：** `paper/历史/round2_第二轮修正.py`（`python3 round2.py` 一键产出）
**日期：** 2026-07-15

---

## 0. 三个用户拍板（AskUserQuestion）

| 事项 | 决定 | 说明 |
|---|---|---|
| 引文 [5] X. Xue/Y. Hu（进化迁移优化） | **保留，改写引用句** | 说明书 Step 5/12 要删；但这是编辑/审稿要求引文。引言改成中性表述"prior search knowledge can further improve its efficiency [5]"，不再暗示本文是迁移优化。 |
| NSGA-III 对比 | **保留** | Table I 保留 NSGA-III 行 + 引文 [8]；它证明"增益来自修复算子而非换算法"。 |
| 式(2) 分母 δ | **保留，待作者核验** | 见 §3 待核验项。 |

**因无删除 → 参考文献编号不变，仍 [1]–[8]（[6] IEEE33、[7] PVGIS、[8] NSGA-III）。**

## 1. 逐 Step 执行（Step 1–12）

- **Step 1 标题**：不改（已含 differentiated / county-level / 110-kV）。
- **Step 2 作者区**：保留 3×2 六作者 IEEE 网格（上一轮已建）。
- **Step 3 摘要**：整段替换 —— `reduce`→**alter supply adequacy**；删 `synthetic`；算子改名 engineering-prior population seeding + feasibility repair；`selected design`→**selected Pareto solution**；修正"84 代达到什么"歧义（"reaches the latter's 800-generation final level in about 84 generations"）。数字不变（13.1%、4.8%、K=40、84 代）。
- **Step 4 关键词**：保留。
- **Step 5 引言**：两段整替；新算子名；删 `synthetic`；**保留 [5]（改写引用句，见 §0）**。
- **Step 6 模型**：6.1 首段压缩 + CLR 数值定义；6.5 删 `N-1 supply risk`→"expected energy not supplied under N-1 contingencies"；6.6 式(4)/(5) 参数说明合并重写（式(5)在两段之间，故仍分两段）；**式(2) 保留 δ**；6.7 符号首现即定义（K/R_j/P_j/L_j^p/r_j^0/β/η/δ/R_220）。
- **Step 7/8 II-B 整体重构**：引导段改为"retains standard variation, non-dominated sorting, crowding-distance selection [4] + engineering-prior population seeding + feasibility repair"；加 `clip`/`[x]+` 定义；**Algorithm 1 改名 "Engineering-Prior Seeding and Feasibility Repair"、用 seeding+repair 伪代码替换原 12 行 NSGA-II 主循环**；AggregateCLRAdjust / FeasibilityRepair 各一句规范说明。
- **Step 9 实验**：Setup 改写（[6] IEEE33、[7] PVGIS、K∈{10,20,30,40}、N=100、800/400 代）；表题→**"TABLE I. NORMALIZED FINAL HV (MEAN OF THREE RUNS)"**；`Warm-start-only`→**`Seeding-only`**（表 + 正文）；结果段重写（含 84 代）。
- **Step 10 应用/图注**：K=20 段改"selected Pareto solution"，保留 188.82（真实造价，见 §3）；图注压缩为"Fig. 1. K=20 differentiated CLR and storage allocation."。**图像去重"no BESS" = 待办（见 §4）**。
- **Step 11 结论**：新算子名；删 `synthetic`。
- **Step 12 参考文献**：PVGIS 补全 URL（https://…）+ 访问日期；保留 [5]、NSGA-III；编号 [1]–[8] 连续、正文↔文末一致（脚本核验）。

## 2. 公式硬约束（§1–§10）达成情况

- **全部数学 = 原生 Word OMML**（pandoc→`<m:oMath>` 节点注入）。终稿：**55 个行内 oMath + 8 个 display oMathPara**。
- 行内符号（K、N、R_j、P_j、L_j^p、r_j^0、β、η、δ、Ω_j、S_{j,s}、K=40、K=20、R=2.0、N=100 等）**全部 OMML**；摘要 9 pt、正文 10 pt、算法 7.5 pt —— 每处 OMML 按所在段字号自动缩放（body sz=20 / abstract sz=18 / algorithm sz=15）。
- **伪代码内数学全部 OMML**：←、∪、∅、~、[·]+、clip、R̃、下标上标等；算子名（Input/Output/for/do/return/end for/AggregateCLRAdjust/FeasibilityRepair）按 §7 允许作粗体或普通文本。
- **本机可核验项全过**（final_verify.py）：文本 run 内无 LaTeX 残串（无 `$`/`\frac`/`\sum`/`\beta`/`_{`/`^{`…）、无 Unicode 上下标冒充、无 `�` 乱码、散文无字面 `X_y` 下标、无孤立公式片段。

## 3. ★待作者核验（模型/数据相关，未擅改，符合说明书结尾"勿改模型"）

1. **式(2) 的 δ=0.85**：已对 `实验/有EA/scripts/county_model.py`/`ea_county.py` 核实 —— `DIVERSITY_FACTOR=0.85` **确在聚合容载比分母** `ΣR_jL_j/(δΣL_j)`（`aggregate_capacity_load_ratio` L112-118；repair L129）。**去 δ 会击穿拐点 1.80→1.53 并需重跑全套 EA** → 保留。请作者最终确认口径。
2. **188.82 million CNY/yr**：核实为**真实年化造价**（`ea_baseline.csv`≈18882 万元），非归一指标 → 保留 "annualized cost"。但其**基于合成 K=20 县（缩放 IEEE33），非徐州实测**。
3. **式(3) 站级/上级闸用 L_j^p（投影峰）**：既往真实性审查发现代码站级/上级闸用**当前峰 L_j**、仅 EENS 用 L_j^p。论文全篇用 L_j^p（自洽）。**这是负荷口径选择，未改（改属模型变更）**，请作者知悉。
4. **AggregateCLRAdjust = 乘法比例缩放（+ 下界未达时加性 top-up）**，已核实代码（`_repair_one` L127-160），**非 projection**（说明书 §8.7 红线）→ 论文如实写 "rescales multiplicatively"。
5. **Algorithm 1 第 5 行 = Pᵢ←0**：代码**储能初始化为 0、在 repair 里定容**（Explore 核实），故比说明书理想伪代码（在 seeding 里给储能公式）**更忠实**；储能定容公式移到第 11 行（repair）。
6. **采样**：代码用 LHS（+25% 播种于可行聚合附近 R=1.53）；论文写 "~U([Rmin,Rmax])" 为抽象，忠实度足够。

## 4. 待办 / 需在 Word 内确认（本机 LibreOffice 无法核验）

- **图 1 去重 "no BESS"**（Step 10 图像压缩）= **待办**：需改图生成脚本 `实验/有EA/scripts/fig_short_paper_matrix.py`（另一独立任务），本轮未动图。如需，请告知。
- **以下项须作者在 Word 内目检**（本机唯一转换器 LibreOffice 对 OMML 过度渲染、字形有怪癖 `∑→⤳`/`[·]+→¿`/小号偏大，不代表 Word）：① 式(1)–(5) 与伪代码可点击进入公式编辑态；② 导出 PDF 无裁切/方框/缺字；③ 页数。本机已核 docx 内 OMML 的 `m:t` 字符正确、无孤立片段、算法各行在放大字号下**均不溢出单元格**（→ Word 7.5 pt 更不会溢出）。
- **页数**：LibreOffice 显示 **3 页**（同前若干轮：其对内联 OMML 过度渲染撑高行/换行）。**内容量本身仍是 2 页级**；按用户指示 **超 2 页由作者手动压缩内容**（说明书 §四压缩优先序：先删重复文字/短伪代码/低价值参考文献，**勿缩正文与公式字号**）。
