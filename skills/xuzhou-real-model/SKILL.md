---
name: xuzhou-real-model
description: Use when implementing, reviewing, testing, mapping, solving, visualizing, or reporting the 徐州110/35 kV 2021—2025真实数据容载比项目 under 实验/研究, including the three-path comparison, annual asset mapping, storage/expansion/downsize optimization, costs, matrices, Word deliverables, and 10 kV local-case interfaces.
---

# 徐州真实数据三路径建模（v3.2）

## 1. 适用范围与迁移守卫

本技能只服务项目 `实验/研究/` 下的真实数据工作。旧 synthetic M1、旧 `real_2025`、旧五年独立 C0/A/B 矩阵只能回归或归档，不能决定 v3 业务定义。

开始工作前先核对 Git/远端 HEAD 与 Actions `head_sha`，再依次完整读取：

1. 根目录 `AGENTS.md`（含第 12 节项目记忆规则）；
2. `实验/研究/model_contract.yaml`、`model_contract_v3_2_overlay.yaml` 和 resolved contract；
3. processed manifest、逐时审批表及 frozen 正式结果 manifest；
4. `实验/研究/docs/IMPLEMENTATION-PLAN-REAL-2021-2025-V3.md`；
5. `实验/研究/docs/REAL-DATA-MODEL-SPEC.md`；
6. `实验/研究/data/tuomin/电网建模数据_Agent整合版_V1.2/README_Agent_建模数据引用与使用说明_V1.2.md`；
7. 涉及储能成本时读取 `参考政策/储能成本依据/来源与适用说明.md`；
8. 最后读取 `memory/INDEX.md` 与 `memory/current.md` 承接当前任务。历史 session 只作追溯。

如果 `model_contract.yaml` 版本低于 `3.2.0`，它是待迁移的旧契约。此时按 `AGENTS.md` 与 v3 实施计划先完成迁移，不得用旧契约否决已批准的 v3.2 定义。

## 2. 不得重新讨论的唯一方案

共同基准是 2021 年实际状态，决策期是 2022—2025 年，价格统一折算为 2025 年。2021 年既有资产对两条优化方案均为共同沉没成本。

正式路径只有：

- `PATH_ACTUAL_2021_2025`：2021—2025 实际，事实对照，不参与最优排名；
- `PATH_OPT_CLR_UNBOUNDED`：不限制容载比优化，以 2021 年实际状态为基准；
- `PATH_OPT_CLR_LE_2`：以**2021 年实际在役资产为共同起点**；存量容量豁免，不为满足 Rcap 反向退役既有资产，从 2022 年起对 110 kV 规划期新增容量施加 `ΔS<=max(2.0×P_plus−S_2021,0)`。35 kV 辅助层不施加逐年上界。`S0=2×P2021` 仅作为标准化反事实敏感性基准。

第三类实验为**弹性扫描**：`Rcap ∈ {1.5, 1.6, …, 3.0, ∞}` 步长 0.1，在 110 kV 正式层以实际资产共同起点逐点求最优演进成本，产出「Rcap—累计 EAC」前沿；局部细化识别经济释放阈值，稳健性只在 Rcap 维度判断。推荐 Rcap 不与实际物理 CLR 取交集；当前不绑定或物理不可行片区不形成数值推荐。

`SCHEME_C0/A/B` 只用于识别 v2 历史结果，不得写入 v3 正式输出。

两条优化方案使用相同的实际资产起点、候选、物理约束、成本库和求解精度；严格方案的额外项为 110 kV 规划期新增容量的逐年 `Rcap=2.0` 控制。同一实际起点且两方案均可行时允许直接比较累计 EAC；不可行对象不形成成本比较。

目标函数是 2022—2025 年**累计年化成本**（累计在役 EAC）最小，同时输出年度新增 CAPEX、年度在役 EAC、累计 CAPEX 和累计在役 EAC。实际路径的设备级成本未闭合时写“未识别”，不能写 0。

## 3. CLR、储能与动作红线

路径 `k`、年份 `y`、时刻 `t`：

```text
P_net = P_actual + P_charge - P_discharge + P_tie
P_plus = max_t(max(P_net, 0))
P_minus = max_t(max(-P_net, 0))
S = S_2021 + 累计扩容 - 累计减容
R = S / P_plus
```

- 负荷源数据按净负荷解释，不能再次扣减现状光伏。
- 正式 CLR 只以路径自身、年度同步、正向最大净负荷为分母；反向峰值与反向承载力单列。
- v2 固定干预前分母已经废止。
- 储能充电只能吸收本时段原有反向功率，不能跨过零点制造正向峰值；放电只能服务本地正向负荷，不能反送或套利。
- 反向承载力按设备级计算：分列/单台 `beta=0.8`；并列组 `beta=min(0.8,(S_total-S_largest)/S_total)`，不能二次乘 0.8。
- 正向容量缺口、反向承载缺口和新增容量 Rcap 包络分别记录触发原因；物理 CLR 大于 2 本身不自动触发扩容或储能。
- 扩容、减容、退役、替换都必须来自可追溯的离散候选；减容成本按同电压、同容量扩容成本对称计价，投资为正，不抵扣残值。
- 主体模型不能出现弃光变量、隐含弃光成本、连续虚构容量或全县 10 kV 联络决策。

## 4. 数据映射工作流

1. 保持 `实验/研究/data/tuomin/` 只读。
2. 派生数据写入 `实验/研究/data/processed/real_2021_2025/`。
3. 保留 `source_ref`、`source_version`、`transformation`、`scenario_id`、`quality_flag` 和输入 SHA-256。
4. 先生成逐年设备白名单，再按该年白名单审批时序列；审批主体为项目负责人。
5. 2025 QX-00005 的 110 kV 运行口径是 20 站、40 台主变；BDZ-00056 两台年末新增设备不阻塞这 40 台的逐时门禁。
6. 第 11、12、27、28 列必须保留“源表头—审批后设备”双字段，不能静默覆盖。
7. 2024 年 `-7319、-6858、16630 MW` 三点隔离，原值不得丢失。
8. 官方县区年度容量与同步峰值是总量锚点；不能把当前在役设备按投运年倒推结果冒充实际历史资产。
9. 静态设备极值不能求和冒充县区同步峰值；经验时长场景只能写“经验短时/中心/长时”，不能写 P50/P90。

站级光伏使用 SRC05；`邳州10kV线路基础数据表.xlsx` 的“装机容量(kVA)”是下挂配变/用户装见容量，不是光伏容量。不得把站级光伏随机分摊到馈线后当作真实点位。

## 5. 测试驱动执行顺序

每个行为变更执行红—绿—重构：先写最小失败测试并确认失败原因正确，再实现，再运行目标测试和相关回归。旧测试不能直接删除；保留有效物理断言，改写已废止的 v2 定义。

按 v3 计划顺序执行：

1. 阶段 0：升级机器契约、模型规格、输出规范、运行手册和契约测试；
2. 阶段 1：跨年时序映射、项目负责人审批、年度资产白名单和实际行动台账；
3. 阶段 2：路径自身 CLR、同步峰值和正反向缺口；
4. 阶段 3：真实离散扩容/减容候选、储能和成本库；
5. 阶段 4：两条 2022—2025 多年度联合优化方案（共同采用 2021 实际资产起点，严格方案仅增加新增容量 Rcap 包络）；
6. 阶段 4b：弹性扫描 `Rcap=1.5~3.0` 步长 0.1 加无上限点，产出 Rcap—成本前沿与推荐区间依据；
7. 阶段 5：110/35 kV 分层模型及两个独立 10 kV 局部案例接口；
8. 阶段 6：两套转置式矩阵、技术附表、Word、manifest 和归档；
8. 阶段 7：全量测试、质量断言和真实端到端验证。

固定环境和工作目录：

```bash
cd 实验/研究
env MPLCONFIGDIR=/tmp/mplcfg_xuzhou conda run -n xuzhou110kv_clr pytest -q tests/test_model_contract_v3.py
env MPLCONFIGDIR=/tmp/mplcfg_xuzhou conda run -n xuzhou110kv_clr bash scripts/runtests.sh
env MPLCONFIGDIR=/tmp/mplcfg_xuzhou conda run -n xuzhou110kv_clr python scripts/run_all.py --dataset real_2021_2025 --config model_contract.yaml --skip-gen
```

不能仅凭进程退出码判断完成；还要审查求解状态、空值、映射门禁、年度容量守恒、严格方案逐年新增容量 Rcap 包络（110 kV）、存量豁免后物理 CLR 的独立计算、两方案齐备性、弹性扫描前沿完整性（每个 Rcap 点均有全片区结果）、110/35 kV 分离和输出哈希。

## 6. 10 kV 局部案例接口

主体模型只预留两个默认关闭、独立失败的入口：

```text
--existing-tie-case-options
--new-tie-line-case-options
```

前者用于现有联络切分，后者用于新建联络线路。默认分别比较；只有显式兼容且依赖满足时才组合。案例必须守恒县区功率，并避免重复计算反向功率、避免扩容量和成本。

## 7. 输出和人工审查

- 110 kV 与 35 kV 分开建模、分开聚合、分开输出矩阵。
- 精确查值源是 CSV/Markdown；Word 是人工审查层。
- 正式 Word 只用一种转置式矩阵：第一列中文指标，后续列项目统一编码片区；说明字段放表外。
- 正式层输出 110 kV 八片区推荐矩阵、35 kV 八片区辅助矩阵；QX-00005 三方案逐年结果放技术附表。
- 不再生成五张正式年度 Word 矩阵；旧矩阵归档并标“旧契约、不得正式引用”。
- 推荐 Rcap 区间来自弹性扫描前沿的 5% 近优带、局部细化和同一 Rcap 维度的参数敏感性；不与实现 CLR 取交集，也不能直接把无上限结果的 R 写成推荐值。当前不绑定或物理不可行片区不形成数值推荐。
- 严格方案的实际资产共同起点、存量豁免和新增容量控制说明必须随矩阵呈现；同一实际起点且均可行时可比较 EAC，不可行对象不填伪成本。

## 8. 自动继续与真正暂停条件

局部数据或候选缺口不能影响其他对象时，记录问题、降低证据并继续。只有下列事项暂停请求用户确认：

- 无法按权威优先级消解的源定义冲突会改变正负方向、容量单位、电压归属或年度口径；
- 必须新增未批准动作、弃光变量或新政策目标；
- 必须改变 2021 共同基准、2025 价格年、累计在役 EAC 目标或 2022 年严格约束起点；
- 两个同等证据映射会改变设备/电压归属，且项目负责人无法依据现有关系表审批。

其他工程决策、字段补充、异常隔离、证据降级、旧测试迁移和局部不可行均自动处理，不再询问用户。

## 9. 完成声明门槛

只有 v3 契约测试、全量测试、数据质量断言和 `real_2021_2025` 端到端均通过，且矩阵、Word、成本分解、问题台账、运行日志、契约快照和 manifest 齐全时，才可声称完成。任何旧 `real_2025`、旧年度矩阵或门禁前运行结果都不是 v3 最终结果。
