# 项目级 Agent 硬约束

当前约束版本：3.4  
定版日期：2026-08-27  
状态：v3 完成门槛已达，遗留负责人终审  
本版变更：§12.5 明确根目录 `claude_session_1.txt` 为负责人确认的最新中断接续记录，在进度、完成状态、遗留项和下一步方面优先于较旧的项目 memory，并要求后续 Agent 读取后刷新 memory。此前 3.3（2026-08-26）：§5.1 TIE-001 按负责人决定不再收资、最终口径"不形成定量结论"；增补六馈线结构化拓扑碎片化事实与转供能力容量包络口径。

## 1. 指令优先级与必读文件

涉及 `实验/研究/` 的分析、实现、测试、映射、求解、出表或报告工作，开始前依次读取：

0. 根目录 `claude_session_1.txt`（负责人确认的最新中断接续记录；用于确认当前进度、已完成项、遗留项和下一步）以及 `memory/INDEX.md` 与 `memory/current.md`（第 12 节；用于承接已登记的长期状态）
1. `实验/研究/docs/IMPLEMENTATION-PLAN-REAL-2021-2025-V3.md`
2. `实验/研究/model_contract.yaml`
3. `实验/研究/docs/REAL-DATA-MODEL-SPEC.md`
4. `skills/xuzhou-real-model/SKILL.md`
5. `实验/研究/data/tuomin/电网建模数据_Agent整合版_V1.2/README_Agent_建模数据引用与使用说明_V1.2.md`
6. 涉及储能成本时读取 `参考政策/储能成本依据/来源与适用说明.md`

冲突优先级：本文件 > v3 `model_contract.yaml` > v3 实施计划 > v3 模型规格 > 数据说明书 > 旧代码、旧结果和早期报告。

迁移期特殊规则：若 `model_contract.yaml` 的版本仍低于 `3.0.0`，说明它尚未完成迁移；此时 `IMPLEMENTATION-PLAN-REAL-2021-2025-V3.md` 优先于旧契约。不得因旧契约仍写 `real_2025`、固定分母或 `SCHEME_C0/A/B` 而回退已批准的 v3 业务定义。

`实验/研究/PLAN-M1.md`、旧合成 M1、旧 `real_2025`、旧五年年度矩阵和旧可视化都不是 v3 方案依据。

## 2. 环境与测试

- 所有实验、数据处理、模型、测试和出表必须使用 Conda 环境 `xuzhou110kv_clr`。
- 环境路径：`/home/xh/anaconda3/envs/xuzhou110kv_clr`。
- 未激活时使用 `conda run -n xuzhou110kv_clr ...`。
- Matplotlib 使用 `MPLCONFIGDIR=/tmp/mplcfg_xuzhou`。
- 所有相对命令以项目内 `实验/研究/` 为工作目录。
- 修改行为前先写能暴露旧口径的失败测试，确认因预期行为缺失而失败，再改实现。

固定全量测试入口：

```bash
env MPLCONFIGDIR=/tmp/mplcfg_xuzhou conda run -n xuzhou110kv_clr bash scripts/runtests.sh
```

v3 真实端到端目标入口：

```bash
env MPLCONFIGDIR=/tmp/mplcfg_xuzhou conda run -n xuzhou110kv_clr python scripts/run_all.py --dataset real_2021_2025 --config model_contract.yaml --skip-gen
```

旧 `--dataset real_2025` 和 synthetic 入口只能作为迁移回归，不能证明 v3 模型正确。

## 3. 唯一研究方案

### 3.1 时间与成本基准

- 共同基准为 2021 年实际状态；严格路径按 §3.2 以归一约定状态为起点。
- 2021 年既有资产为共同沉没成本，不计入增量成本。
- 决策期为 2022—2025 年。
- 所有路径统一按 2025 年价格计价。
- 单目标为 2022—2025 年累计在役等效年成本（EAC）最小；同时输出年度 CAPEX、年度在役 EAC、累计 CAPEX 和累计在役 EAC。

### 3.2 路径代码

正式输出、日志、文件名和图表只使用：

- `PATH_ACTUAL_2021_2025`：2021—2025 实际，事实对照，不参与最优排名。
- `PATH_OPT_CLR_UNBOUNDED`：不限制容载比优化，以 2021 年实际状态为共同基准。
- `PATH_OPT_CLR_LE_2`：控制容载比不超过 2.0 优化，以**初始基准年容载比 2.0 的约定状态为起点**（2026-08-24 定版）：起点容量 `S0 = min(S_2021, 2 × min(P_2021, min决策年峰))`，仅出清冗余容量、峰值保持 SRC08 官方锚点原值，起点调整不计入决策期成本；从 2022 年起逐年施加 `R<=2.0` 仅作用于 110 kV 正式层。35 kV 辅助层因年度峰值剧烈波动（回落年最低至基准年 34%）不施加逐年上界，与第 5 节辅助定位一致。

`SCHEME_C0`、`SCHEME_A`、`SCHEME_B` 仅用于识别和归档 v2 结果，不得进入 v3 正式产物。没有新的 A/B/C 简称。

两条优化路径使用相同候选、物理约束、成本库和求解精度；严格路径的额外项为归一起点与逐年 `R<=2.0`（仅 110 kV）。正式中文成本字段统一写“累计年化成本”。由于严格路径起点为约定归一状态、与不限制路径起点不同，两路径 EAC 表征各自口径下的最优演进成本，不得做跨口径增量比较或宣称其中一路径成本更低；违反路径齐备性、逐年约束或口径标注要求视为实现或求解错误，结果不得发布。

## 4. 容载比与物理红线

- 源主变负荷按净负荷解释，不得再次扣减现状光伏。
- 正式容载比只使用同电压等级公用变容量除以该路径、该年度、同步、正向最大净负荷。
- 反向峰值和反向承载力单列；不得把正反向峰值取大后称为正式容载比。
- 每条路径按自身干预后净负荷重新计算正向峰值和容载比；v2 的固定干预前分母规则已经废止。
- 储能充电只能吸收本时段原本存在的反向功率，不得跨过零点制造正向峰值来降低容载比。
- 储能放电只能服务本地正向负荷，不得形成反送或无约束套利。
- 反向承载力在设备级计算：分列/单台 `beta=0.8`；并列组 `beta=min(0.8,(S_total-S_largest)/S_total)`，不得再乘一次 0.8。
- 正向容量缺口和反向承载缺口是措施触发原因；容载比大于 2.0 本身不自动触发扩容或储能。
- 扩容、减容、退役和替换必须来自可追溯的离散候选，不得使用连续虚构容量。
- 减容后必须重新通过正向容量、反向承载、运行方式和网络压力检查。
- 减容成本暂按同电压、同容量扩容成本对称计价，CAPEX 为正，不产生负成本或残值抵扣。
- 主体模型不设置弃光变量或弃光成本；物理不可行时必须明确标为不可行。
- N-1 只作内部容量网络压力筛查；缺少阻抗时不得声称完成精确 AC/DC 潮流。

## 5. 电压、空间和 10 kV 边界

- 110 kV 与 35 kV 分开建模、分开聚合、分开输出；不得跨电压相加容量、负荷、光伏或成本。
- 110 kV 输出八县区正式推荐矩阵；35 kV 输出八县区辅助矩阵，设备明细留在技术底稿。
- 只有父级映射、同步转移量和候选完整时才允许跨层联合枚举，防止重复治理和重复计费。
- 主体模型不做全县 10 kV 联络优化。
- 后续局部案例分为 `现有10 kV联络切分案例` 与 `新建10 kV联络线路案例`，独立接入、独立审批、独立失败。
- 两类案例默认分别比较；只有显式兼容且依赖满足时才允许组合。
- 审批后的局部案例可回接两条优化路径，用于改变站间负荷分布和避免部分扩容/储能，但不得伪造县区总功率。

### 5.1 10 kV 联络案例数据基线与代理值规则（2026-08-24 增补）

- `实验/研究/data/tuomin/10kV_AgentReady_V5_2.zip` 是 10 kV 联络案例的唯一数据基线；源 zip 只读，解包工作副本放 `实验/研究/data/tuomin/10kv_case/`。其自带 `final_preflight_qa.csv` 的 11 项门禁是案例侧机器契约测试的断言来源。
- 10 kV 局部案例采用 110 kV 站级等值源端建模；“馈线→10 kV 母线段→具体主变”仅在未来做单台主变级 N-1 归因时才成为必填输入，当前不是数据缺口。
- 河东线规划 P 采用 `√3×10 kV×2025 最大电流×0.95` 代理（源值 0 判异常缺失）；所有引用该值的结果必须携带 proxy 标记，获得真实同步有功后方可覆盖。
- 支路有效热限固定为 `min(导线 Imax, SRC06 馈线最大允许电流)`；仅当某具体开关成为绑定约束且无铭牌值时，输出“定点复核该开关”，不得扩大收资范围。
- TIE-001~008 正常状态按 V5.2 `MODEL_CONFIG.yaml` 的 confirmed_ties 为准（TIE-007 CLOSED，其余 OPEN）。跨站联络定量结论仅 TIE-002（4.75 MW）与 TIE-003（V5_2 数据复算 3.25 MW）；**TIE-001 按负责人 2026-08-26 决定不再收资，最终口径为"不形成定量结论"**，断点明细保留在 `tie_master.csv` 与问题台账，如后续获得图纸可按同一引擎复算。
- 六馈线结构化拓扑为碎片森林（杆段记录缺失，负荷 seed 源端可达率 0%~100%）：S1/S2 电压类证据仅对源端可达分量成立，跨站联络转供后电压与支路负载校核不可实现；转供能力数字一律按容量包络口径引用并携带受端同时段余量前置条件（2026-08-26 重分析定版，详见 `实验/研究/分析/2026-08-26-10kV联络案例重分析_收资定稿口径.md`）。

## 6. 数据映射与证据

- `实验/研究/data/tuomin/` 中源文件只读，不得覆盖或原地清洗。
- v3 标准化数据写入 `实验/研究/data/processed/real_2021_2025/`。
- 派生表保留 `source_ref`、`source_version`、`transformation`、`scenario_id`、`quality_flag` 和输入 SHA-256。
- 映射审批主体为项目负责人，不再要求源数据负责人审批。
- 正式逐时证据门禁按年度资产白名单执行：只有该年实际运行口径中的设备需要全部获批。
- 2025 QX-00005 110 kV 运行口径为 20 座站、40 台主变；BDZ-00056 两台年末新增设备不进入该运行口径，不得阻止其余 40 台获得正式逐时资格。
- 时序映射必须保留源表头和审批后目标设备。第 11、12、27、28 列的表头冲突不得静默覆盖。
- 2024 年 `-7319、-6858、16630 MW` 三个异常值必须隔离并留存原值、时间、列号和质量标记。
- 2021—2025 官方县区容量、同步峰值和容载比是年度总量锚点；不能只按当前在役设备投运年份回溯后冒充实际历史资产。
- `PATH_ACTUAL_2021_2025` 的实际措施成本只有在设备级毛动作闭合后才能计算；未知成本不得写 0。
- 静态站级年度极值不得相加冒充县域同步峰值。
- 非 QX-00005 县区使用官方县峰锚点和经验短时/中心/长时情景时，证据不高于 `EVIDENCE_B`；不得使用 P50/P90 表述。
- `EVIDENCE_A/B/C` 只表示证据等级，不表示优化路径。

## 7. 光伏和馈线字段

- 站级光伏容量已知，未知的是 10 kV 馈线内具体点位。
- `邳州10kV线路基础数据表.xlsx` 的“装机容量(kVA)”确定为下挂配变/用户装见容量，不是光伏容量。
- 该字段不得乘光伏曲线、不得作为光伏容量、不得作为线路允许输送能力。
- 光伏容量来自 SRC05；其主要为 2026 快照，与 2025 或更早负荷组合时必须标记跨年口径。
- 不得随机分摊站级光伏到馈线后当作真实点位；随机或约束撒点仅用于明确标注的局部敏感性案例。

## 8. 输出与人工审查

- 旧五张年度正式矩阵作废并归档，不删除证据，不得与 v3 结果混放。
- 正式精确查值源为 CSV/Markdown，Word 为人工审查层。
- 正式 Word 仅使用一种转置式矩阵：第一列为中文指标，后续列为脱敏片区；说明字段移到表前或表后。
- 分别输出 110 kV 和 35 kV 两套矩阵。
- QX-00005 三路径逐年轨迹、措施和成本分解放技术附表，不再制作五张正式年度 Word 矩阵。
- 机器字段、哈希、JSON、候选编号和长备注不得混入甲方主表。
- 正式主表至少显示推荐容载比区间、三路径 2025 容载比、三路径累计成本、严格约束增量成本、正向/反向缺口、缺口设备数、推荐措施和证据等级。
- 推荐区间来自不限制路径成本最小可行结果及其经过验证的敏感性结果，不得机械取五个独立年度最优值的最小—最大。

## 9. 自动继续与暂停规则

映射、资产、光伏、候选或成本缺口不影响其他对象时，自动记录问题、降低证据等级并继续。只有以下情况暂停：

- 源数据冲突会改变正负方向、容量单位、电压归属或年度口径，且无法按权威优先级消解；
- 需要引入未批准的动作类型、弃光变量或新政策目标；
- 需要改变 2021 共同基准、2025 价格年、累计在役 EAC 目标或 2022 年严格约束起点；
- 同一时序列存在两个同等证据且会改变设备或电压归属的候选，项目负责人无法依据现有关系表审批。

除上述事项外，后续不再要求用户确认，持续执行测试、映射、建模、求解和交付。

## 10. 完成声明门槛

声明完成前必须验证：

- v3 契约测试和全量测试通过；
- `real_2021_2025` 端到端入口通过；
- 年度资产、映射、异常修正和实际行动可追溯；
- 两路径各自路径齐备、口径标注明确；由于严格路径从归一约定状态起步，不作两路径累计 EAC 的跨口径包含关系比较；
- 严格路径逐年 `R<=2.0`；
- 110/35 kV 未混算；
- 无弃光、伪成本、伪 CLR 或未标记插补；
- 两套矩阵、Word、技术附表、问题台账和 manifest 齐全。

## 11. 研究报告语言与文风硬约束

- 第一至第三章及后续研究报告正文，优先继承项目既有成熟材料的语言表达、论证顺序和工程报告文风；《服务报告-初稿》第一、二章和《睢宁县魏集镇独立储能电站容量规划报告》为主要文风参照。
- 技术口径需要按 v3 修正时，应在保留原文自然表达的基础上改写，不得因补充模型定义而新增模板化、机械化或明显带有 AI 痕迹的表述。
- 禁止高频使用“首先、其次、再次、最后、综上所述、值得注意的是、在……背景下”等套路连接词；禁止强行凑成三点、段落机械对称、空泛总结、营销化形容和无数据支撑的程度判断。
- 正文应以具体对象、数据、规范或计算关系承担逻辑衔接。句式长短应有变化，段落篇幅允许不均；必要的编号和分项仅用于确有并列关系的研究范围、方法、步骤或结论。
- 不得为降低 AI 痕迹而改动专业术语、公式、数据、引用、图表编号、标准名称和模型口径，不得编造事实或引用。
- 凡涉及研究报告起草、续写、合并或润色，自动启用 `de-ai-humanizer` 的中文技术报告规则进行中度语言复核；每章完成后至少执行一次“模板词、连接词密度、句长起伏、段落规律性、主语完整性、事实与引用保真”审查，再进入 Word 生成。
- 用户后续提供的原文改写示范和表述黑名单，优先于通用风格建议，并应追加记录到本节或项目专用文风清单中，供后续会话自动加载。
- **报告工作强制挂载（2026-08-25）**：凡涉研究报告起草、续写、合并、润色或审查，必须先加载 `skills/xuzhou-report-writing/SKILL.md`，按其六步工作流执行，并完成 S1–S4 四步去 AI 味审查后方可交付。
- **v3.1 口径红线**：严格路径起点必须表述为“初始基准年容载比 2.0 的归一约定状态”（记账口径、峰值保持官方锚点、不计决策期成本）；两路径 EAC 跨口径不可比较；推荐容载比区间来自弹性扫描前沿近优带；逐年 R≤2.0 仅作用于 110 kV 正式层。
- **交付门槛**：每章交付物必须附带已登记的来源与审查清单（沿用 `研究报告/初稿/第一至第三章来源与审查清单.md` 格式），并完成 Word→PDF 渲染闭环审查。
- **全代号输出红线（2026-08-25 负责人增补）**：研究报告及一切面向甲方的输出文档中，变电站名称、线路名称和区县名称一律以项目统一编码表达（片区 QX-XXXXX、站点 BDZ-XXXXX、线路 PZXL-XXXXX、联络案例 TIE-XXX）；正文不得出现具体站名、线名和县区地名；地级市层面的"徐州地区"仅限报告标题与研究范围表述。真实名称与设备明细只保留在技术底稿，正式查值源 CSV 已使用编码的保持不变。

## 12. 项目记忆（memory/）自动存储与读取

`memory/` 是本项目跨会话状态的唯一权威位置，服务于模型切换、会话中断和长任务交接。目录规范见 `memory/README.md`。

### 12.1 启动读取（强制）

新会话在执行第 1 节任务前，须按以下顺序读取：根目录 `claude_session_1.txt`，再读取 `memory/INDEX.md` 和 `memory/current.md`，确认当前状态、已完成项和下一步优先级。`claude_session_1.txt` 是项目负责人确认的最新中断接续记录；就当前进度、已完成项、遗留项和下一步而言，其优先级高于较旧的 `memory/INDEX.md` 与 `memory/current.md`。不得用较旧 memory 覆盖其中的最新事实。若记录缺失或仍存在无法消解的冲突，先停止业务操作并向项目负责人确认，不得自行虚构状态。

### 12.2 写入时机（自动，无需用户确认）

出现下列任一情况必须立即写入，不等提醒：

- 开始新任务或子任务：更新 `current.md` 的状态字段与"下一步"；
- 任务状态改变、进入阻塞或从阻塞恢复；
- 关键验证完成：登记实际命令、退出码、计数和证据路径；
- 会话结束或上下文即将耗尽：更新 `current.md`，并在 `memory/sessions/YYYY-MM-DD/HHMM-主题.md` 追加本次记录（只增不改）。

状态取值固定为 `PLANNING`、`IMPLEMENTING`、`VERIFYING`、`BLOCKED`、`INCOMPLETE`、`COMPLETE`。

### 12.3 写入内容标准

每条记录必须能回答：在做什么、完成了什么、还缺什么、有何阻塞、执行过哪些命令及退出码、证据文件路径、下一步是什么。无法由文件、命令输出或哈希支持的表述只能标记为假设。

- 已确认且影响后续实现的长期决策追加到 `memory/decisions/`，并同步 AGENTS.md、`model_contract.yaml` 或 Skill 等权威文件；
- 大型输出只登记路径、大小、SHA-256 与退出码，不入库；
- 不保存 Token、凭据、个人隐私或未脱敏的甲方敏感数据；
- `current.md` 只保留最新快照；历史一律进 `sessions/`，不得覆盖旧记录。

### 12.4 与既有规则的关系

- 本节与第 1 节同等级；按本节更新记忆属于第 9 节的自动继续范围，不需要暂停或请求确认。
- 根目录 `NEXT-SESSION-PROMPT.md` 是接手指令模板；`memory/next-session-prompt.md` 维护其与实际进度的差异。状态事实以 `memory/current.md` 为准。
- 本项目 `.git` 曾确认不可用：记忆以文件本身为准，关键产物登记 SHA-256，不虚构提交记录；若日后 git 可用，可在记忆更新后附加提交，但不得以提交替代文件内容。

### 12.5 负责人确认的最新历史会话优先级（2026-08-27）

- 根目录 `claude_session_1.txt` 是当前项目最新的 Claude 接续记录。项目负责人已确认：它在进度事实、任务完成状态、遗留事项和下一步安排方面优先于较旧的 `memory/INDEX.md`、`memory/current.md` 和根目录提示词。
- 上述优先级仅用于接管和进度统一，不改变模型、物理、数据和报告约束。`AGENTS.md`、`model_contract.yaml` 及其同步后的负责人决策仍是业务口径权威；历史会话中明确提出的口径变化，必须同步写入相应权威文件后才生效。
- 后续 Agent 读取 `claude_session_1.txt` 后，须立即将确认的最新状态刷新到 `memory/current.md`，并在 `memory/sessions/YYYY-MM-DD/` 新增记录；不得让旧 memory 再次成为接手依据。
