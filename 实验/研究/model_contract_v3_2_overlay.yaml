# v3.2 版本标记层。
#
# 业务语义全部以 model_contract.yaml 为 canonical source。本文件只保留
# 版本覆盖机制所需的元数据；不得在这里重复 planning、physics、data 或
# output schema，以免直接读取 base 与 resolved contract 得到不同定义。
contract:
  version: "3.2.0"
  status: "frozen"
  frozen_at: "2026-08-29"
  change_note: "主政策实验改为2021实际资产共同起点、存量容量豁免且Rcap只约束决策期新增容量；保留2×P2021标准化起点为二级敏感性基准；分离Rcap与实际CLR；增加连续8760h SOC和beta敏感性"
  overlay_role: "version_marker_only"
  base_contract_version_required: "3.2.0"
