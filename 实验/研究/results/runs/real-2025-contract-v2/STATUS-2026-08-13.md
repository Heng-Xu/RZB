# real-2025-contract-v2 状态说明

该运行目录是 2026-08-10 门禁前的历史运行证据。候选时序映射当时尚未审批，当前代码已在正式 C0/A/B 求解前增加审批门禁；因此本目录中的矩阵不能直接作为本次更新后的正式推荐结果。

本次更新的主要变化：

- C0 物理可行时才允许作为零成本当前案例推荐；
- 增加正向容量缺口、反向承载缺口、缺口设备数和措施触发约束；
- 严格 `R<2.0` 另行要求改变存量并产生增量成本；
- 年度 Word 主表固定为 15 行转置式结构。

新的正式运行必须在时序映射审批完成后使用唯一入口重新生成。
