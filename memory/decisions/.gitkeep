此目录存放已确认且影响后续实现的长期决策记录，文件名格式：YYYY-MM-DD-主题.md。

写入时机：某决策经用户确认或按 AGENTS.md 自动确认规则生效，且会约束后续会话的实现选择时。
写入要求：记录决策内容、确认方式（用户明示 / 依据哪条规则自动确认）、生效日期、影响的权威文件（AGENTS.md、model_contract.yaml、Skill 等）。

注意：本目录只做登记索引，权威定义仍以被同步的文件为准；两者冲突时以后者为准并回改此处。
