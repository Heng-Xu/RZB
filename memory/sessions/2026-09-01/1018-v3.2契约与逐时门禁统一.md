# v3.2 契约与逐时门禁统一

时间：2026-09-01 10:18（Asia/Shanghai）
状态：`IMPLEMENTING`

## 完成

- 将 `model_contract.yaml` 设为 v3.2 唯一业务语义来源；overlay 仅保留版本标记元数据。
- resolved loader 增加 overlay 语义漂移拒绝、前沿 schema、共同基线、无退役、成本可比和范围限定逐时审批断言。
- 正式逐时状态与 processed manifest 对齐：QX-00005 2025 年 110 kV 运行范围 40/40 台正式批准；16 列 35 kV context-only、2 列年末设备不进入正式门禁。
- 候选映射重放仍输出未审批候选，不因当前契约已获范围批准而自我审批。

## 验证

| 命令 | 退出码 | 结果 |
|---|---:|---|
| `python -m pytest -q tests/test_v32_contract_consistency.py tests/test_v32_contract.py tests/test_model_contract_v3.py tests/test_project_contract.py` | 0 | 22 passed in 0.86 s |
| `python -m pytest -q tests/test_timeseries_mapping.py tests/test_v3_stage1_mapping.py` | 0 | 12 passed in 173.78 s |

两条命令均通过 `conda run -n xuzhou110kv_clr` 执行，`MPLCONFIGDIR=/tmp/mplcfg_xuzhou`。

## 下一步

补齐 v3.2 Actions 依赖策略、手动调度和验证 SHA 记录，然后隔离旧年度模型入口。
