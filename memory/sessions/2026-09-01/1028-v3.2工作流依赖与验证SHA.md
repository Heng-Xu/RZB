# v3.2 工作流依赖与验证 SHA

时间：2026-09-01 10:28（Asia/Shanghai）
状态：`IMPLEMENTING`

## 完成

- 建立 `.github/v32-dependency-policy.yaml`，由静态测试校验 8 个 v3.2 workflow 的触发路径。
- 快速模型 CI 覆盖模型、测试、契约、关键逐时门禁和 workflow 治理文件；重型流程覆盖 canonical contracts、processed 输入及真实传递依赖。
- 所有 workflow 增加 `workflow_dispatch.target_ref`；checkout 后把 `git rev-parse HEAD` 写入 `V32_VALIDATION_COMMIT_SHA`。
- baseline、frontier point、sensitivity scenario、frontier/refinement/sensitivity 聚合及 frozen builder 均登记验证 SHA。
- chronology preflight 改用当前 `run_v32_actual_baseline()`，不再读取旧 v3 动作或生成退役候选。
- 新增 frozen package verification workflow，校验 contract/dataset/hash、正式产物哈希和 jointly feasible 成本包含关系。

## 验证

| 命令 | 退出码 | 结果 |
|---|---:|---|
| `python -m pytest -q tests/test_v32_workflow_dependencies.py tests/test_v32_validation_provenance.py tests/test_v32_repository_governance.py tests/test_v32_formal_entrypoint.py tests/test_v32_frontier.py tests/test_v32_sensitivity_parameters.py tests/test_v32_contract.py tests/test_v32_contract_consistency.py` | 0 | 30 passed in 1.49 s |
| `python -m py_compile ...` 与 8 个 workflow YAML 解析 | 0 | 全部通过 |

## 下一步

为旧 `run_annual_model.py` 增加默认拒绝执行守卫并证明正式入口不可调用它；随后开始冻结结果复现。
