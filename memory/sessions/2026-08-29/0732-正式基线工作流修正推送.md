# 正式基线工作流修正推送

日期：2026-08-29（Asia/Shanghai）
状态：`VERIFYING`

## 事实

- 在 `model-v3.2-autonomous-review` 分支，修正 `.github/workflows/v32-formal-baseline.yml` 对已废弃 `recommendation_matrix_v32_base.csv` 的读取，改为当前 v3.2 入口实际生成的 `policy_2025_summary.csv`。
- 新增回归测试，确保正式工作流读取入口真实产物；旧状态下测试退出码 1，修正后定向测试 4 passed。
- 固定全量测试在 `xuzhou110kv_clr` 环境退出码 0：`251 passed, 14 warnings`，耗时 521.69 s。
- 提交 `708a503643df85e166b8f6680c9b1c7d73856d7b` 已由 `git push origin model-v3.2-autonomous-review` 成功上传；`git ls-remote` 返回同一 SHA。

## 远端复验

- 新正式基线 run `33220722649`：当前为 In progress。
- 新模型验证 run `33220722628`：当前为 In progress。
- 先前实际资产主运行 `33219378228`、chronology preflight `33219378236`、Rcap frontier `33219378257` 已 Success。

## 下一步

等待新一轮正式基线和模型验证结束，记录结论；若均通过，将同步根目录 memory 并把项目状态改为 `COMPLETE`。若有模型或数据失败，重新打开终审，不以本地测试替代远端失败。
