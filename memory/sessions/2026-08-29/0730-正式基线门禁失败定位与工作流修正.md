# 正式基线门禁失败定位与工作流修正

日期：2026-08-29（Asia/Shanghai）
状态：`VERIFYING`

## 触发事实

- 推送 `71d63615c8bc10ea3dac5b0bfe3f445c72e905dd` 后，GitHub Actions `v3.2 formal baseline run`（run `33219378290`）结论为 Failure，工作流页面只给出进程退出码 1，未开放逐步日志。
- 本地按工作流同一入口运行 `src.v32_pipeline.run_v32_pipeline`，工作目录 `实验/研究`，环境 `xuzhou110kv_clr`，退出码 0，生成 `policy_cost_comparison.csv`、`policy_2025_summary.csv` 和 `qx00005_chronology_comparison.csv`。
- 本地复现工作流后处理时确认失败点：工作流读取 `recommendation_matrix_v32_base.csv`，但 v3.2 实际资产入口已不再生成该旧文件；当前入口生成的是 `policy_2025_summary.csv`。

## 修正与验证

- 先新增 `tests/test_v32_formal_entrypoint.py::test_formal_baseline_workflow_reads_only_entrypoint_outputs`；在旧工作流下按 `PYTHONPATH=.` 执行，退出码 1，暴露旧文件引用。
- 将 `.github/workflows/v32-formal-baseline.yml` 的“recommendation matrix”读取改为“2025 policy summary”，保留成本对比和 QX-00005 chronology 检查。
- 修正后的定向测试：4 passed，退出码 0。
- 固定全量测试：`env MPLCONFIGDIR=/tmp/mplcfg_xuzhou conda run -n xuzhou110kv_clr bash scripts/runtests.sh`，退出码 0，`251 passed, 14 warnings`，521.69 s。

## 当前判断与下一步

这是 CI 工作流与当前 v3.2 产物契约不一致，不是模型入口、数据或求解失败。修正尚未形成新的远端提交；下一步提交并推送，等待新的正式基线 Actions 结果。正式结果目录仍以已生成并审查的 `real-2021-2025-v32-frozen` 为准，在新门禁结论前不扩展“全部远端门禁通过”的表述。
