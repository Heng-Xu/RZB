# Git 上传范围审计

时间：2026-08-27 14:20（Asia/Shanghai）
状态：`PLANNING`

## 任务

接管中断的 Claude 会话，以项目根目录建立 Git 跟踪并上传到
`https://github.com/Heng-Xu/RZB`；核心工作目录为 `实验/研究/`，后续主线为依托建模结果撰写研究报告。

## 已确认

- 已读取 `memory/INDEX.md`、`memory/current.md`、`AGENTS.md`、v3 实施计划、
  `model_contract.yaml`、真实数据模型规格、`skills/xuzhou-real-model/SKILL.md`、
  数据说明书和 `memory/README.md`。
- 既有项目记忆记录：v3 建模、研究报告七章初稿、Word/PDF 渲染闭环、10 kV
  联络重分析及 207 项全量回归均已完成；剩余负责人版式终审。
- `claude_session_1.txt` 位于项目根目录，记录了中断前的接续核验和最终收尾过程。
- 根目录不是可用 Git 工作树：`git status --short --branch`、
  `git rev-parse --show-toplevel`、`git remote -v`、`git log --oneline --decorate -8`
  均退出码 128；现有 `.git/` 只有空的 `config.worktree`。
- GitHub API 查询退出码 0，显示 `Heng-Xu/RZB` 为公开空仓库（`private: false`、
  `size: 0`、默认分支 `main`）；`git ls-remote` 退出码 0 且无分支输出。
- 根目录约 360 MB，`实验/研究/` 约 205 MB；唯一超过 GitHub 普通 Git 单文件
  限制的文件为 `参考文献/前沿调研.tar.gz`，大小 171,513,560 bytes。
- 根目录 `Tushare.ma` 为本地凭据文件：只记录存在及后续必须排除，不读取、不提交其内容。
- `.claude/settings.local.json`、锁文件、`.cc-writes`、`__pycache__`、
  `.pytest_cache` 和 `paper/.gstack` 属于机器/运行时状态，不应进入版本库。

## 待确认范围

远端当前为公开仓库。项目中包含脱敏建模源数据、研究报告及 Word/PDF、下载的参考文献
和历史会话。推送前需项目负责人确认是否允许这些材料公开；无论确认结果如何，
`Tushare.ma` 和机器运行时文件都必须排除。超过 100 MB 的参考文献归档可因与解包目录
重复而排除，若需保留原归档则需另行使用 Git LFS/发布附件方案。

## 已执行命令及证据

- `date '+%Y-%m-%d %H:%M:%S %Z'`：退出码 0，时间为 2026-08-27 14:20:10 CST。
- 项目文件读取和目录盘点：退出码 0；证据包括 `AGENTS.md`、`memory/current.md`、
  `claude_session_1.txt`、`实验/研究/README.md` 及 `实验/研究/results/`。
- `git lfs version`：退出码 1，当前环境未安装 Git LFS。
- 凭据模式扫描：未发现私钥或常见 token 字面量；`Tushare.ma` 单独列为本地凭据排除项。

## 下一步

等待公开材料范围确认；确认后建立 `.gitignore`、README/项目地图（如有必要）、初始化
本地 Git、审查暂存清单，提交并以 SSH/HTTPS 可用凭据推送到 `main`。不得覆盖远端已有
历史（当前远端为空）。
